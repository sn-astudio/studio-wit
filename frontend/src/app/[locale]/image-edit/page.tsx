import { Header } from "@/components/Header";

export default function ImageEditPage() {
  return (
    <div className="h-screen overflow-hidden">
      <Header />
      <main className="flex items-center justify-center pt-16 h-full">
        <p className="text-muted-foreground">이미지 편집 (준비중)</p>
      </main>
    </div>
  );
}
